### Contains Duplicate

## Given an integer array nums, return true if any value appears more than once in the array, otherwise return false.

Ex: 01

```
Input: nums = [1, 2, 3, 3]

Output: true
```

First problem here in Brute Force Logic

- the O(n²) that was expensive
